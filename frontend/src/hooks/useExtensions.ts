// Re-export from context for convenience
export { useExtensions, ExtensionProvider } from '../context/ExtensionContext';
export type {
    Extension,
    InstalledExtension,
    ExtensionManifest,
    UIManifest,
    SidebarItem,
    ButtonItem,
    ComponentItem,
    AutomationManifest,
    FileProcessorManifest,
    UIExtensionComponents,
    AutomationJob,
} from '../context/ExtensionContext';

