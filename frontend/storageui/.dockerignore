# Build artifacts and deps (reinstalled/rebuilt inside the image)
node_modules
.next
out

# Local env and secrets — never bake into the image
.env
.env.local
.env.*.local

# VCS and editor
.git
.gitignore
.zed
.vscode
.claude

# Caches and logs
.eslintcache
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store

# Docker
Dockerfile
.dockerignore
